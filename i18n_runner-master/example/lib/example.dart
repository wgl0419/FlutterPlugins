library example;

import 'package:example/generated/i18n_keys.dart';

/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
  final a = I18nKeys.desc;
  final b = I18nRawKeys.desc;
}
